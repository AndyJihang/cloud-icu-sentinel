FROM python:3.10-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit requests pandas

COPY src/frontend/ /app/src/frontend/

EXPOSE 8501

CMD ["sh", "-c", "streamlit run src/frontend/app.py --server.port=${PORT:-8501} --server.address=0.0.0.0"]
